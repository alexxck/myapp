<div class="container">
<%= form_for([@post, @post.comments.build]) do |f| %>
  <% if session[:author_id] != current_user %>
   <p>
    <%= f.label :body %><br>
    <%= f.text_area :body %>
  </p>
  <br>
  <p>
    <%= f.submit({:class => 'btn btn-success'}) %>
  </p>
  <% end %>
<% end %>
</div>