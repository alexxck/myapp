<div class="container">
<%= form_for([@post, @post.comments.build]) do |f| %>
  <p>
    <%= f.label :body %><br>
    <%= f.text_area :body %>
  </p>
<br>
  <p>
    <%= f.submit({:class => 'btn btn-success'}) %>
  </p>
<% end %>
</div>